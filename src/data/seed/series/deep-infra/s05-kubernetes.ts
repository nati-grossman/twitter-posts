import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'מה זה Kubernetes (ניהול מערכות בקנה מידה)?'

export const deepInfraKubernetes: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'הבעיה שפתר', `יש לכם מאה containers רצים איפשהו.

איפה כל אחד רץ? מי נפל? צריך עוד עותק עכשיו, באמצע הלילה?

לעשות את זה ידנית — כמעט בלתי אפשרי, ובטח לא בקנה מידה רציני.

Kubernetes הוא מנצח התזמורת של הקופסאות.

הוא מחליט, מתקן, ומשאיר את המערכת רצה בקנה מידה.

בלי שתשבו על כל קופסה בנפרד כל בוקר.`),

    seriesPost(2, SERIES, 'Cluster', `Cluster של Kubernetes בנוי משני חלקים עיקריים שעובדים יחד.

Control plane — המוח: מחליט מה צריך לרוץ ואיפה, ושומר על המצב הרצוי.

Nodes — ה-workers: המכונות שמריצות את ה-Pods בפועל, על החומרה עצמה.

מוח + ידיים.

בלי שניהם אין תזמורת אמיתית.
רק רעש של מכונות בלי מנצח.
שני החלקים חייבים להיות שם.`),

    seriesPost(3, SERIES, 'Pod', `Pod היא היחידה הקטנה ביותר ב-Kubernetes — נקודת ההתחלה של הכול.

לפעמים container אחד בלבד.
לפעמים כמה שרצים יחד, כי הם צריכים זה את זה מקרוב.

הם חולקים רשת, וחיים ומתים יחד — כמו חבילה אחת קטנה.

חשבו על קופסה קטנה שהמערכת מזיזה ממקום למקום לפי הצורך.

לא אתם מזיזים.
היא — וזה בדיוק העניין.`),

    seriesPost(4, SERIES, 'Deployment', `אתם מגדירים: רוצים חמישה עותקים של האפליקציה חיה תמיד.

Kubernetes שומר על זה.
נפל אחד? הוא מחליף — בלי שתתעוררו בלילה עם טלפון.

Deployment זה החוזה: "כך תיראה המערכת, וכך היא תישאר".

והוא דואג שזה יישאר כך — גם כשמשהו נשבר באמצע היום.

אתם אומרים כמה.
הוא שומר על כמה — גם כששקט וגם כשלא.`),

    seriesPost(5, SERIES, 'Service ו-Ingress', `Pods משתנים כל הזמן — כתובות באות והולכות, עותקים עולים ויורדים בשקט.

Service נותן כתובת יציבה פנימית אליהם, כדי שהשאר לא ירדופו אחרי IP משתנה.

Ingress הוא הכניסה מבחוץ — לרוב עם HTTPS מסודר למשתמשים.

ככה העולם החיצוני לא צריך לרדוף אחרי Podים בודדים שמתחלפים.

יש דלת קבועה למערכת שמשתנה כל הזמן.
זה מה ששומר על השקט.`),

    seriesPost(6, SERIES, 'ConfigMap Secret', `הגדרות וסודות לא צריכים לשבת בתוך ה-image עצמו.

ConfigMap להגדרות רגילות.
Secret לסודות — סיסמאות, מפתחות, דברים שלא רוצים ב-Git לעולם.

מוזרקים בזמן ריצה לסביבה של ה-Pod.

אפשר לסובב סיסמה בלי build חדש של כל התמונה מההתחלה.

הפרדה נקייה בין קוד לבין סביבה.
זה חוסך כאב ראש גדול מאוד.`),

    seriesPost(7, SERIES, 'לא חובה לכולם', `סטארטאפ קטן? לפעמים PaaS פשוט מספיק לגמרי — בלי דרמה.

Kubernetes מביא כוח — וגם מורכבות, תפעול, ועקומת למידה ארוכה.

הוא מתחיל להשתלם כשיש הרבה שירותים, וצוות שיודע לתפעל אותו באמת.

לא חובה רק כי כולם מדברים עליו בטוויטר השבוע.

לפעמים פשטות מנצחת.
ובצדק גמור — במיוחד בהתחלה של המוצר.`),

    seriesPost(8, SERIES, 'Managed K8s', `EKS, GKE, AKS — Kubernetes מנוהל אצל הספקים הגדולים בענן.

הספק מפעיל את ה-Control plane — המוח — בשבילכם, בלי שתתקינו אותו לבד.

אתם מביאים Nodes ו-YAML, ומתמקדים במה שרץ למשתמשים.

פחות כאב ראש על המוח עצמו.

עדיין יש עבודה על מה שרץ, על רשת, ועל הגדרות.
הוא לא קסם בלי עבודה בכלל.`),

    seriesPost(9, SERIES, 'Helm', `Helm זה כמו מנהל חבילות ל-Kubernetes — apt או npm של התזמורת.

תבניות מוכנות: monitoring, מסד נתונים, ועוד כלים נפוצים שכולם צריכים בסוף.

במקום לכתוב הכול מאפס ב-YAML ארוך — מתקינים חבילה ומתאימים לצרכים שלכם.

שוק של פתרונות מוכנים לתזמורת.

חוסך זמן — אם מבינים מה החבילה באמת עושה בפנים, לא רק מתקינים בעיניים עצומות.`),

    seriesPost(10, SERIES, 'סיכום', `Kubernetes = אורקסטרציה של containers בקנה מידה גדול.

רוצה כמה עותקים, מחליף מה שנפל, ומנהל רשת וכניסה מבחוץ בצורה מסודרת.

זה לא "הענן" עצמו.
זה כלי שרץ בענן — או אצלכם on-prem בחדר השרתים.

חזק מאוד כשצריך אותו.
לא תמיד הצעד הראשון הנכון.

לפעמים כדאי לגדול אליו, לא לקפוץ ישר למים העמוקים.`),
  ],
}
