import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'מה זה container?'

export const deepInfraContainer: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'קופסה לריצה', `Container זה חבילה: האפליקציה יחד עם התלויות שלה בפנים.

היא רצה בנפרד על השרת — לא מתערבבת בקלות עם אפליקציות אחרות לידה על אותה מכונה.

כמו קופסה לריצה — סגורה, ניידת, ומוכנה להזזה ממקום למקום בלי דרמה.

אותה קופסה יכולה לרוץ אצלכם, אצל עמית, ובענן — באותו אופן.

פחות "אצלי זה עובד".
יותר אותו דבר בכל מקום באמת.`),

    seriesPost(2, SERIES, 'לא VM מלא', `זה לא מחשב וירטואלי מלא עם Windows או Linux שלם בפנים.

Container חולק את ה-kernel של Linux עם המארח — אין מערכת הפעלה שלמה לכל אחד.

לכן הוא קל יותר.
ולרוב עולה תוך שניות, לא דקות של המתנה כמו מכונה שלמה.

פחות משקל — יותר צפיפות על אותה חומרה פיזית.

אותו שרת מריץ יותר עבודה שימושית.
פחות בזבוז על OS.`),

    seriesPost(3, SERIES, 'Image', `Image זו תבנית קפואה של הקופסה — המתכון לפני הבישול.

כותבים Dockerfile, עושים build, ומקבלים image מוכן להרצה בכל מקום.

שומרים ב-registry — מחסן של תמונות מוכנות.

משם מושכים ומריצים — אותו דבר בכל מקום, בלי הפתעות.

ה-image הוא מה שמעבירים בין מכונות.
ה-container הוא מה שרץ עכשיו.`),

    seriesPost(4, SERIES, 'Docker', `Docker הוא השם הכי מוכר בעולם הזה — כמעט מילה נרדפת לקופסאות.

docker run — והקופסה רצה, כמעט כמו לפתוח אפליקציה רגילה על המחשב.

הרעיון: פיתוח מקומי קרוב מאוד ל-production, בלי פערים מטורפים.

לא תמיד זהה במאה אחוז — יש הבדלים קטנים לפעמים בין סביבות.

אבל הרבה יותר קרוב מלפנים, כשכל מחשב היה סביבה אחרת לגמרי.`),

    seriesPost(5, SERIES, 'Layers', `Image בנוי משכבות — כמו עוגה עם קומות שיושבות אחת על השנייה.

שינוי קטן בקוד לא חייב לבנות הכול מחדש מההתחלה כל פעם.

יש cache לשכבות שלא השתנו — והן חוזרות מהר מהזיכרון.

Build מהיר יותר — ופחות בזבוז זמן ורוחב פס בהעלאות.

סדר השכבות ב-Dockerfile משפיע על זה מאוד.
שווה לחשוב עליו לפני שממהרים.`),

    seriesPost(6, SERIES, 'Port mapping', `בתוך ה-container האפליקציה מאזינה, נניח, על פורט 3000 בפנים.

מבחוץ ממפים ל-8080 — או לכל פורט שתרצו לפתוח בדפדפן שלכם.

ככה הדפדפן מגיע פנימה בלי לערבב את הרשת של המארח עם כל הקופסאות ליד.

גשר קטן בין העולם החיצוני לקופסה הסגורה.

פשוט — וקריטי.
בלי זה, אף אחד לא מגיע פנימה מהחוץ.`),

    seriesPost(7, SERIES, 'Volumes', `Database שרץ רק בתוך container — והנתונים נמחקים כשהוא נעלם?

Volumes שומרים קבצים על הדיסק של המארח, מחוץ לקופסה עצמה לגמרי.

הקופסה יכולה להתחלף מחר.
הנתונים נשארים במקום.

חשוב במיוחד לכל דבר עם state — לא רק לקוד זמני שרץ ונעלם בלי זיכרון.

הקופסה זמנית.
הנתונים לא צריכים להיות.`),

    seriesPost(8, SERIES, 'docker-compose', `API + DB + Redis — כמה containers יחד שעובדים כצוות.

docker-compose מרים אותם מקובץ אחד, עם רשת ביניהם מוכנה מראש.

נוח מאוד לפיתוח מקומי בלי כאב ראש.

סביבה שלמה — בלי להתקין הכול ידנית על המחשב, חבילה אחרי חבילה במשך שעות.

לחיצה אחת, והסטאק חי.
זה חוסך שעות של התקנות משעממות.`),

    seriesPost(9, SERIES, 'Production', `Docker לבד לא תמיד מספיק בקנה מידה גדול עם הרבה שירותים.

צריך אורקסטרציה: איפה לרוץ, כמה עותקים, מה עושים כשמשהו נופל באמצע הלילה.

שם נכנס Kubernetes וכלים דומים שמנהלים את הקופסאות יחד.

הקופסה היא הבסיס.
בלי בסיס אין מה לתזמר.

התזמורת היא השלב הבא ב-production.
בלי תזמורת — רק קופסאות בודדות.`),

    seriesPost(10, SERIES, 'סיכום', `Container = חבילה עקבית לריצת אפליקציה בכל מקום.

קלה, מהירה, וניידת — מקומי, ענן, ובין חברים בצוות באותו יום.

הפתרון ל"על המחשב שלי זה עובד": אותו image בכל מקום באמת.

פחות הפתעות בין סביבות שגורמות לכאב ראש בפריסה.

מכאן מתחילים הרבה מערכות מודרניות.

הקופסה היא נקודת ההתחלה — לא סוף הסיפור.`),
  ],
}
