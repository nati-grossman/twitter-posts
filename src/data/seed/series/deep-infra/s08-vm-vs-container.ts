import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'מה ההבדל בין VM ל-container?'

export const deepInfraVmVsContainer: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'VM — מחשב וירטואלי', `VM זה מחשב וירטואלי מלא — כאילו יש מכונה נפרדת על השולחן.

Hypervisor מריץ Windows או Linux שלם על חומרה וירטואלית, עם כל מה שמגיע עם זה.

בידוד חזק בין מכונות.
וגם כבד יותר: כל VM נושא מערכת הפעלה משלו על הגב.

כמו דירה שלמה בתוך בניין — עם קירות עבים באמת.

יש מקום.
יש מחיר במשאבים.`),

    seriesPost(2, SERIES, 'Container — תהליך מבודד', `Container משתף את אותו kernel עם המארח — לא OS מלא לכל אחד.

באמצעות namespaces ו-cgroups הוא מבודד תהליכים ומשאבים — בלי מערכת הפעלה שלמה בפנים.

רק מה שצריך לאפליקציה נכנס לקופסה הקלה.

קל יותר מ-VM — ועדיין מופרד יחסית מהשכנים.

פחות דירה שלמה עם קירות.
יותר חדר מאורגן היטב בבניין משותף.`),

    seriesPost(3, SERIES, 'זמן הפעלה', `VM יכול לקחת דקות לעלות — לאתחל מערכת הפעלה שלמה לוקח זמן אמיתי.

Container — לרוב שניות, לפעמים פחות.

לכן scale עם containers מרגיש מהיר יותר תחת עומס פתאומי של משתמשים.

פחות המתנה כשמוסיפים עותקים באמצע היום העמוס.

זמן הפעלה הוא לא פרט קטן בטבלה.
הוא משפיע על איך המערכת גדלה באמת.`),

    seriesPost(4, SERIES, 'משאבים', `עשרה VMs = בערך עשרה kernels שרצים במקביל.

יותר בזבוז זיכרון ומעבד על מערכות הפעלה שרק יושבות ברקע ואוכלות משאבים.

עשרה containers חולקים kernel אחד של המארח.

אפשר לצפיפות גבוהה יותר על אותה חומרה פיזית.

אותו שרת פיזי משרת יותר עבודה שימושית.
פחות "מערכת הפעלה על מערכת הפעלה" סתם.`),

    seriesPost(5, SERIES, 'אבטחה', `ב-VM, לברוח ממכונה אחת לאחרת קשה יותר — הבידוד עמוק, עם קירות עבים באמת.

ב-Container יש shared kernel — וזה משנה את התמונה.

צריך הגדרה נכונה, עדכונים, והרשאות מצומצמות — לא להשאיר הכול פתוח בטעות.

אם רופפים — הסיכון גדול יותר ממה שנשמע.

קל ומהיר לא אומר בטוח אוטומטית.
צריך לטפל בזה ברצינות.`),

    seriesPost(6, SERIES, 'מתי VM', `תוכנות Windows ישנות, רגולציה קשה, או צורך בשליטה מלאה על ה-OS מלמעלה עד למטה.

שם VM עדיין נפוץ מאוד — ולא בגלל נוסטלגיה לטכנולוגיה ישנה.

לא הכול מוכן לקופסאות קלות היום.

לפעמים צריך מחשב שלם — גם אם הוא וירטואלי לגמרי.

זו לא טכנולוגיה מתה.
יש לה מקום ברור בשוק — וגם היום, לא רק פעם.`),

    seriesPost(7, SERIES, 'מתי container', `מיקרו-שירותים, CI, פריסות תכופות, ו-scale מהיר תחת עומס.

שם containers הפכו לסטנדרט מודרני — כמעט ברירת מחדל בצוותים חדשים.

אותה חבילה בכל מקום.
פחות "אצלנו זה אחרת" בין מפתחים.

כשהאפליקציה בנויה לחבילות קטנות — זו הבחירה הטבעית לגמרי.

קלה להזיז.
קלה לשכפל.
וזה משנה הכול בפריסה.`),

    seriesPost(8, SERIES, 'יחד', `הרבה פעמים לא בוחרים או-או כמו בתחרות.

VMs מריצים containers — שכבה על שכבה, אחת מעל השנייה.

שכבה כפולה: בידוד של המכונה + צפיפות של הקופסאות הקלות.

נפוץ מאוד בענן של הספקים הגדולים.

כל שכבה פותרת בעיה אחרת.

לא צריך לבחור צד אחד לנצח במלחמה מדומה בין השתיים.

שניהם חיים יחד בשקט.`),

    seriesPost(9, SERIES, 'Serverless', `לפעמים מתחת ל-Serverless רץ container — אתם פשוט לא רואים אותו.

אתם לא רואים אותו בממשק.
הספק מנהל את השכבה הזו בשבילכם לגמרי.

אתם כותבים פונקציה — והקופסה שקופה מאחורי הקלעים.

אותו רעיון של חבילה, בלי שתנהלו אותה ידנית בכלל.

יש קופסה.
פשוט לא אתם מחזיקים אותה בידיים — והספק כבר שם.`),

    seriesPost(10, SERIES, 'סיכום', `VM = OS מלא, כבד יותר, בידוד חזק בין מכונות.

Container = קל, משותף kernel, מהיר להפעלה תוך שניות.

לא תמיד בחירה אחת נגד השנייה כמו משחק סכום אפס.

לעיתים שניהם — כל אחד בשכבה שלו, זה מעל זה בענן.

השאלה היא איזה בידוד ומהירות אתם באמת צריכים עכשיו.

לא מה נשמע חדש יותר בטוויטר השבוע.`),
  ],
}
