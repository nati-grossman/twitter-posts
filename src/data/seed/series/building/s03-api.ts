import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'מה זה API?'

export const buildingApi: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'הגדרה', `API — Application Programming Interface —
הוא דרך סטנדרטית שמערכות מדברות אחת עם השנייה.

במקום לגעת ישירות בקוד או במסד נתונים,
משתמשים ב"דלת" מוגדרת מראש.

הלקוח שולח בקשה ל-API.
השרת מטפל.
וחזרה מגיעה תשובה.

פשוט.
מסודר.
ובלי לחפור בפנים של הצד השני.`),
    seriesPost(2, SERIES, 'מסעדה', `האנלוגיה הכי טובה ל-API היא מסעדה.

אתה הלקוח.
המלצר הוא ה-API.
המטבח הוא המערכת הפנימית.

אתה לא נכנס למטבח לבשל לבד.
אתה מבקש מהמלצר.
והמלצר מחזיר את התוצאה.

כך גם תוכנות:
מבקשות דרך דלת ברורה —
בלי לדעת איך כל סיר עובד מאחור.`),
    seriesPost(3, SERIES, 'REST נפוץ', `REST הוא סגנון API מאוד נפוץ באינטרנט.

הוא נשען על בקשות HTTP פשוטות.

למשל:
GET למשתמש מסוים,
POST להתחברות,
DELETE למחיקת פוסט.

כל פעולה היא בקשה ברורה לנתיב ברור.

קל להבין.
קל לבדוק.
ולכן נפוץ כל כך במערכות ווב.`),
    seriesPost(4, SERIES, 'Request ו-Response', `כל API עובד באותו מקצב בסיסי.

יש Request — מה ששולחים.
יש Response — מה שחוזר.

למשל שולחים מזהה משתמש,
ומקבלים שם וגיל בחזרה.

השרת לא צריך להכיר את כל המסך שלך.
רק את החוזה של הבקשה והתשובה.

ברגע שיש חוזה —
שני הצדדים יכולים להשתנות בנפרד.`),
    seriesPost(5, SERIES, 'Public vs Private', `לא כל API פתוח לכולם.

Public API נגיש יותר החוצה —
למשל מזג אוויר, או חלקים מ-Google Maps.

Private API נשאר פנימי לחברה —
תשלומים פנימיים, שירותי backend, לוגיקה עסקית.

אותו רעיון של דלת.
רק עם מפתחות שונים לקהלים שונים.

השאלה היא לא רק "יש API?" —
אלא למי מותר לדפוק עליו.`),
    seriesPost(6, SERIES, 'GraphQL', `GraphQL עובד אחרת מ-REST הקלאסי.

במקום לקבל חבילה קבועה מראש,
מבקשים בדיוק את השדות שצריך.

למשל שם משתמש, ורק כותרות של פוסטים.

היתרון:
פחות שליפה מיותרת.
ויותר גמישות לצד שמציג את המידע.

לא תמיד צריך הכול.
לפעמים צריך בדיוק מספיק.`),
    seriesPost(7, SERIES, 'Webhooks', `לפעמים לא רוצים לשאול שוב ושוב: "קרה משהו?"

במקום זה משתמשים ב-Webhook.

המערכת השנייה שולחת הודעה אליכם כשאירוע קורה —
תשלום הצליח, משתמש נרשם, הזמנה עודכנה.

פחות סקרנות מיותרת.
יותר "תקראו לי כשצריך".

אותו עולם של APIs —
רק עם כיוון הפוך של השיחה.`),
    seriesPost(8, SERIES, 'תיעוד (Documentation)', `API טוב מגיע עם תיעוד.

מה ה-endpoints.
מה שולחים.
מה חוזר.
ומה הדוגמאות שעובדות באמת.

בלי מסמך כזה — גם API חזק כמעט לא שמיש.

כי אנשים לא מנחשים חוזים.
הם קוראים אותם.

תיעוד טוב חוסך ימים של ניסוי וטעייה.`),
    seriesPost(9, SERIES, 'גרסאות', `APIs משתנים עם הזמן.

לכן רואים נתיבים כמו /v1/users ו-/v2/users.

המטרה פשוטה:
לא לשבור מערכות שכבר בנויות על הגרסה הישנה.

אפשר לשפר.
אפשר להוסיף.
ואפשר לתת לגרסה הישנה לחיות עוד קצת —
בלי לנתק את מי שעדיין תלוי בה.`),
    seriesPost(10, SERIES, 'סיכום', `API הוא הגשר בין אפליקציה למערכת.

הוא מאפשר הפרדה בין חלקים,
שימוש חוזר,
אבטחה טובה יותר,
וגדילה מסודרת בלי להדביק הכול יחד.

במילים פשוטות:

API הוא הדרך הסטנדרטית שבה תוכנות מדברות באינטרנט —
בלי לדעת איך הצד השני בנוי מבפנים.

דלת ברורה.
חוזה ברור.
ושיחה שעוברת.`),
  ],
}
