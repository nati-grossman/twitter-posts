import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'איך שני אתרים מדברים אחד עם השני?'

export const buildingSitesTalk: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'לא כבל ישיר', `שני אתרים לא מחוברים בכבל ישיר כמו שני מחשבים על שולחן.

הם מדברים דרך האינטרנט —
בבקשות HTTP או HTTPS.

כל אתר הוא שרת שיודע לקבל בקשה ולהחזיר תשובה.

אין קו פרטי ביניהם.
יש פרוטוקול משותף מעל הרשת.

וזה בדיוק מה שמאפשר לאתרים זרים לגמרי
לשתף פעולה בלי לגור באותו חדר.`),
    seriesPost(2, SERIES, 'דוגמה: תשלום', `אתר קניות רוצה לחייב כרטיס אשראי.

הוא לא מטפל בכסף לבד.
הוא שולח בקשה לספק תשלום כמו Stripe או PayPal.

מקבל תשובה.
ומעדכן שההזמנה שולמה.

האתר מבקש שירות.
הספק מבצע את החלק הרגיש.

וכך שתי מערכות משתפות פעולה —
בלי שהחנות תבנה בנק בתוך עצמה.`),
    seriesPost(3, SERIES, 'CORS', `כשדפדפן מאתר אחד מנסה לדבר עם API של אתר אחר —
הוא בודק הרשאה.

האם מותר לאתר הזה לפנות אליי?

הבדיקה הזו נקראת CORS.

אם אין אישור — הדפדפן חוסם.

זה לא תמיד אומר שהשרת נפל.
לפעמים זו פשוט הגנה של הדפדפן,
שמוודא שהשיחה בין אתרים קורה רק כשמותר.`),
    seriesPost(4, SERIES, 'Server-to-server', `בין שרת לשרת השיחה חופשית יותר.

אין דפדפן באמצע עם מגבלות CORS.

Backend אחד פונה ל-Backend אחר —
לשלוח מיילים, למשוך נתונים, או להפעיל שירות חיצוני.

עדיין צריך אבטחה ומפתחות.
אבל המסלול טכני יותר ישיר.

פחות שכבות בדפדפן —
יותר שיחה בין מערכות מאחורי הקלעים.`),
    seriesPost(5, SERIES, 'OAuth', `כשלוחצים Login with Google —
האתר שלכם לא מקבל את הסיסמה.

במקום זה יש תהליך OAuth:
האפליקציה עוברת דרך Google,
המשתמש מאשר,
וחוזר Token.

Google אומרת בעצם: כן, זה המשתמש הזה.

בלי לחשוף סיסמה לאתר השלישי.

זהות מאושרת —
בלי להעביר את המפתח עצמו.`),
    seriesPost(6, SERIES, 'Embed', `לפעמים אתר אחד פשוט מוטמע בתוך אחר.

סרטון YouTube בתוך דף.
מפת Google.
ווידג'ט צ'אט.
או טופס תשלום.

זה עדיין תקשורת בין מערכות —
רק דרך iframe או סקריפט שיושב בתוך הדף.

נראה כמו חלק מהאתר שלך.
מאחורי הקלעים זה עדיין מישהו אחר שמגיש תוכן.`),
    seriesPost(7, SERIES, 'Webhooks', `Webhook הופך את הכיוון.

במקום שהאתר שלכם ישאל כל הזמן "יש עדכון?",
השרת השני דופק אליכם כשמשהו קורה.

למשל Stripe שולח הודעה כשתשלום הצליח.

פחות סקרנות מיותרת.
יותר תגובה בזמן אמת.

במקום לרדוף אחרי אירועים —
נותנים לאירועים להגיע אליכם.`),
    seriesPost(8, SERIES, 'SDK', `לפעמים מקבלים SDK — ערכת כלים מוכנה.

במקום לכתוב HTTP ידנית לכל פעולה,
קוראים לפונקציה כמו תשלום דרך הספרייה.

מתחת למכסה זה עדיין בקשות רשת לאותו API.

רק שהחיים של המפתח נעשים נוחים יותר.

פחות טקס של בקשות.
יותר קוד שנשמע כמו כוונה.`),
    seriesPost(9, SERIES, 'שגיאות וריטרי', `תקשורת בין אתרים לא תמיד מצליחה בניסיון הראשון.

Timeout.
שגיאת 500.
כשל רשת.

לכן מערכות משתמשות ב-retry,
ב-backoff,
ולפעמים ב-fallback.

לא כי הן פסימיות —
כי הרשת האמיתית לא מושלמת.

מערכת טובה יודעת גם להיכשל יפה,
ואז לנסות שוב בחוכמה.`),
    seriesPost(10, SERIES, 'סיכום', `שני אתרים "מדברים" דרך HTTP מעל האינטרנט.

יש APIs.
יש הרשאות כמו OAuth או מפתחות.
יש CORS בדפדפן.
ויש Webhooks, SDKs ו-Embeds כשצריך.

הרעיון המרכזי:
אין חיבור ישיר קסום —
יש פרוטוקולים ומנגנונים שמאפשרים שיתוף מידע בצורה מבוקרת.

זה הבסיס לתשלומים, התחברות, מפות ווידאו —
ולכל האינטרנט המודרני שבנוי משיחות כאלה.`),
  ],
}
