import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'מה זה authentication vs authorization?'

export const buildingAuthnAuthz: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'שתי מילים, שני תפקידים', `יש שני מושגים שקל לערבב.

Authentication שואל: מי אתה?

Authorization שואל: מה מותר לך לעשות?

נשמע דומה.
עובד אחרת לגמרי.

אחד מזהה.
השני מרשה.

ברגע שמפרידים ביניהם —
הרבה באגים של אבטחה פתאום נעשים ברורים יותר.`),
    seriesPost(2, SERIES, 'דוגמת מועדון', `דמיינו מועדון.

בכניסה לדלת בודקים מי אתה — Authentication.

בפנים מחליטים לאיזה אזורים מותר להיכנס — Authorization.

השומר קודם מזהה,
ואז פותח או סוגר דלתות פנימיות.

אותו רעיון במערכות.

קודם יודעים מי עומד מול הדלת.
רק אחר כך מחליטים לאן מותר לו ללכת.`),
    seriesPost(3, SERIES, 'Login', `Login הוא Authentication.

אימייל וסיסמה —
או דרך אחרת להוכיח זהות.

אם זה הצליח, המערכת יודעת מי המשתמש.

עדיין לא דיברנו מה מותר לו לעשות.
רק מי עומד מול הדלת.

זה הצעד הראשון —
לא כל הסיפור.`),
    seriesPost(4, SERIES, 'Roles', `Authorization מתחיל כשמחלקים תפקידים.

משתמש רגיל.
מודרטור.
אדמין.

למשל משתמש יכול לקרוא פוסטים,
ואדמין יכול גם למחוק משתמשים.

אותה זהות —
הרשאות שונות.

הכניסה הצליחה לכולם.
החדרים בפנים לא.`),
    seriesPost(5, SERIES, 'Token מכיל מה', `אחרי Login מגיע Token.

לפעמים הוא נושא איתו מזהה משתמש ותפקיד.

כך המערכת יכולה להבין בבקשה הבאה
מי מדבר — ומה בכלל מותר לו.

הטוקן לא מחליף בדיקות.
הוא עוזר להן לקרות מהר יותר.

כמו תג שם על החולצה —
לא מפתח לכל החדרים.`),
    seriesPost(6, SERIES, '401 vs 403', `שני קודי שגיאה שמספרים סיפור שונה.

401 — לא התחברת בכלל.

403 — התחברת, אבל הפעולה אסורה עליך.

אחד אומר "מי אתה?"
השני אומר "לא לך."

זה ההבדל הקלאסי בין הזדהות להרשאה.

וכשרואים אותם נכון —
קל יותר לתקן את הצד הנכון.`),
    seriesPost(7, SERIES, 'Scope ב-OAuth', `ב-OAuth יש Scopes — הרשאות מדויקות.

לקרוא אימייל.
לכתוב פוסטים.
לגשת ליומן.

המשתמש לא נותן "הכול" סתם.
הוא נותן חלקים.

כך האפליקציה מקבלת רק מה שבאמת צריך.

פחות כוח מיותר —
פחות סיכון מיותר.`),
    seriesPost(8, SERIES, 'שגיאה נפוצה', `טעות נפוצה:
לחשוב שאם מישהו מחובר — מותר לו הכול.

אבל Login זה לא Permissions.

Authentication לא שווה Authorization.

מחובר עדיין יכול להיות מוגבל מאוד —
וזה דווקא סימן טוב.

כי מערכת בטוחה לא מערבבת
"זיהיתי אותך" עם "הכול פתוח".`),
    seriesPost(9, SERIES, 'Zero Trust', `עקרון מודרני אומר: אל תסמוך אוטומטית על כלום.

גם אם המשתמש התחבר —
עדיין בודקים הרשאות לכל פעולה.

כל בקשה נבדקת מחדש.

כי זהות לבד לא מספיקה.
צריך גם אישור לפעולה הספציפית.

לא חוסר אמון אישי —
זהות ומדיניות בכל צעד.`),
    seriesPost(10, SERIES, 'סיכום', `Authentication שואל מי אתה.
Authorization שואל מה מותר.

הזרימה הטיפוסית:
Login → Token → בקשה → בדיקת הרשאות → אישור או סירוב.

בפועל זה כמו מועדון:

Authentication פותח את הדלת.
Authorization מחליט לאיזה חדרים מותר להיכנס.

שתי שאלות.
שתי תשובות.
ואז המערכת באמת יודעת מה לעשות.`),
  ],
}
