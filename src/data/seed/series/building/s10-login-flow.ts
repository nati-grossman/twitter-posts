import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'איך מערכת התחברות באמת עובדת מקצה לקצה?'

export const buildingLoginFlow: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'המסע המלא', `מערכת התחברות היא שרשרת של שלבים.

הרשמה.
Login.
אימות זהות.
Session או JWT.
בקשות עם הרשאה.
ולבסוף Logout.

המטרה היא לא רק לדעת מי המשתמש.

אלא לעשות את זה בצורה מאובטחת — שוב ושוב,
בכל בקשה, לא רק ברגע הכניסה.`),
    seriesPost(2, SERIES, 'הרשמה', `בהרשמה המשתמש שולח אימייל, שם וסיסמה.

השרת בודק תקינות,
מוודא שאין משתמש כזה כבר,
ושומר את הרשומה במסד הנתונים.

חשוב במיוחד:
הסיסמה עצמה לא נשמרת כמו שהוקלדה.

נשמר ייצוג מוגן שלה.

כי גם אם מישהו יגע במסד —
הסיסמה המקורית לא אמורה לחכות שם.`),
    seriesPost(3, SERIES, 'בדיקת סיסמה', `בהרשמה הסיסמה עוברת Hash ונשמרת כך.

ב-Login מקלידים שוב סיסמה,
מריצים אותה דרך אותה פונקציית Hash,
ומשווים לתוצאה השמורה.

אם זהה — המשתמש מאומת.

השרת לא צריך לזכור את הסיסמה המקורית.

הוא רק יודע להשוות תוצאות Hash —
וזה מספיק לאימות.`),
    seriesPost(4, SERIES, 'Session vs JWT', `אחרי Login צריך לזכור שהמשתמש מחובר.

בגישת Session הדפדפן מקבל מזהה,
והשרת שומר את מצב ההתחברות אצלו.

בגישת JWT המידע והחתימה יושבים בתוך הטוקן עצמו,
והשרת מאמת את החתימה.

שתי דרכים לזכור זהות —
עם איזונים שונים של שליטה, תוקף ופשטות.

אותה מטרה.
שני מודלים.`),
    seriesPost(5, SERIES, 'Cookie מאובטח', `ברוב האתרים השרת מחזיר Cookie עם מזהה Session.

כדי להקטין סיכונים מוסיפים דגלים כמו:

HttpOnly.
Secure.
SameSite.

הם לא פותרים הכול לבד.

אבל הם מקשים על גניבה ועל התקפות נפוצות בדפדפן.

Cookie בלי הגנות כאלה —
נשאר חשוף יותר ממה שצריך.`),
    seriesPost(6, SERIES, '2FA', `סיסמה היא שכבה אחת.

2FA מוסיף שכבה נוספת —
אפליקציית Authenticator,
SMS (פחות מומלץ),
או מפתח אבטחה פיזי.

גם אם הסיסמה דלפה,
לתוקף עדיין חסר הגורם השני.

שכבה מעל שכבה.

לא כי סיסמה מיותרת —
כי שכבה אחת לבד כבר לא מספיקה.`),
    seriesPost(7, SERIES, 'Refresh Token', `טוקנים לא אמורים לחיות לנצח.

יש Access Token קצר-חיים,
ולפעמים Refresh Token ארוך יותר.

כשהראשון פג —
משתמשים בשני כדי לקבל חדש.

כך מצמצמים נזק אם Access Token נגנב,
בלי לבקש Login בכל כמה דקות.

תוקף קצר למה שרץ הרבה —
ותוקף ארוך יותר למה שמחדש אותו.`),
    seriesPost(8, SERIES, 'Logout', `Logout נשמע פשוט — ויש לו כמה וריאציות.

ב-Session מוחקים את ה-Session בשרת.

ב-JWT לפעמים מבטלים טוקן,
ולפעמים מחכים שיפוג.

המטרה זהה:
שהזהות הישנה לא תמשיך לעבוד אחרי שהמשתמש יצא.

כי "יצאתי" צריך להיות אמיתי —
לא רק כפתור יפה במסך.`),
    seriesPost(9, SERIES, 'OAuth Flow', `Login with Google הוא תהליך OAuth.

האפליקציה שלכם עוברת דרך Google.
המשתמש מאשר.
חוזר Token.
ואז האפליקציה יודעת שהזהות אושרה.

האתר שלכם לא רואה את סיסמת Google.

Google רק מאשרת: זה באמת המשתמש הזה.

נוח למשתמש.
ובטוח יותר מלהעביר סיסמה לאתר שלישי.`),
    seriesPost(10, SERIES, 'סיכום מקצה לקצה', `מערכת התחברות מודרנית מחברת כמה שכבות.

הרשמה עם Hash לסיסמה.
Login.
Session או JWT.
Cookie או Token מאובטחים.
לפעמים 2FA ו-Refresh Token.
ולפעמים OAuth.

כשלוחצים "התחבר",
מאחורי הקלעים רצים מנגנוני אבטחה ואחסון —
כדי לזהות אותך מחדש בכל בקשה, בצורה אמינה.

זה לא קסם של כפתור אחד.
זו שרשרת שלמה שעובדת בשקט.`),
  ],
}
