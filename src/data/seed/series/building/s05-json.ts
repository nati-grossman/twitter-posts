import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'מה זה JSON?'

export const buildingJson: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'פורמט טקסט', `JSON הוא פורמט טקסט פשוט להעברת מידע בין מערכות.

השם אומר JavaScript Object Notation —
אבל בפועל כמעט כל שפה יודעת לקרוא ולכתוב אותו.

לא קובץ מסתורי.
לא פרוטוקול נפרד.

פשוט טקסט מסודר שכולם מסכימים עליו.

ולכן הוא הפך לשפה משותפת של הרבה מערכות באינטרנט.`),
    seriesPost(2, SERIES, 'מבנה', `JSON בנוי ממפתחות וערכים.

למשל שם וגיל בתוך אובייקט עם סוגריים מסולסלים.

יש אובייקטים.
יש מערכים.
ויש מחרוזות, מספרים, true/false ו-null.

המבנה קריא לעין —
וגם נוח למחשבים לפרסר בלי דרמה מיותרת.

פשוט מספיק לבני אדם.
מדויק מספיק לקוד.`),
    seriesPost(3, SERIES, 'למה לא HTML', `HTML מיועד להצגה — איך דף נראה.

JSON מיועד לנתונים — מה המידע עצמו.

אחד בונה מסך.
השני מעביר עובדות בין מערכות.

לכן כש-API מחזיר משתמש או רשימת מוצרים,
בדרך כלל זה JSON — לא HTML.

כי כאן לא צריך עיצוב.
צריך עובדות מסודרות.`),
    seriesPost(4, SERIES, 'ב-API', `JSON הוא הסטנדרט השקט של הרבה APIs.

ה-Frontend שולח בקשה.
ה-Backend מחזיר JSON.
והמסך יודע מה לעדכן.

סטטוס, רשימת מספרים, אובייקט מקונן —
הכול יכול לשבת בתוך אותה תשובה טקסטואלית.

בלי JSON כזה,
השיחה בין צדדים הייתה מסורבלת בהרבה.`),
    seriesPost(5, SERIES, 'parse ו-stringify', `ב-JavaScript יש שני תרגומים חשובים.

JSON.stringify הופך אובייקט לטקסט.
JSON.parse הופך טקסט חזרה לאובייקט.

כי ברשת נוסעים תווים.
ובקוד רוצים מבנים שאפשר לגעת בהם.

שני הכיוונים האלה הם גשר יומיומי.

בלי תרגום כזה —
המידע נשאר תקוע כמחרוזת או כאובייקט, בלי לעבור.`),
    seriesPost(6, SERIES, 'שגיאות נפוצות', `JSON קשוח יותר ממה שנדמה.

מפתח בלי מרכאות — לא חוקי.
פונקציות — אסור.
הערות בתוך הקובץ — גם לא.

חייבים מרכאות על מפתחות,
ומבנה נקי בלי הפתעות.

טעות קטנה אחת —
והפרסור נשבר.

לכן JSON מרגיש פשוט,
אבל דורש דיוק.`),
    seriesPost(7, SERIES, 'XML ישן', `לפני ש-JSON נהיה נפוץ כל כך — היה XML.

תגיות בתוך תגיות.
יותר כבד.
וקשה יותר לקריאה יומיומית.

לכן ברוב השימושים המודרניים JSON תפס מקום.

לא כי XML נעלם לגמרי —
כי JSON פשוט הרגיש קל יותר לעין ולידיים.

פחות רעש.
יותר מידע.`),
    seriesPost(8, SERIES, 'YAML, protobuf', `JSON לא לבד בעולם הפורמטים.

YAML קריא מאוד לבני אדם — נפוץ בהגדרות.

Protobuf דחוס ומהיר יותר,
ומשמש למשל ב-gRPC,
אבל פחות ידידותי לעין.

כל פורמט בוחר איזון אחר בין קריאות לבין יעילות.

JSON יושב יפה באמצע —
ולכן כל כך נפוץ.`),
    seriesPost(9, SERIES, 'גודל', `JSON קל לקריאה — אבל לא תמיד הכי קטן.

במערכות גדולות חוסכים רוחב פס עם gzip,
או עוברים לפורמטים דחוסים יותר כמו protobuf ו-msgpack.

היופי של JSON נשאר:
פשטות ושקיפות.

כשצריך לחיצה על הביטים — יש כלים אחרים.

כשצריך להבין מה עובר בשיחה —
JSON עדיין מנצח בהרבה מקומות.`),
    seriesPost(10, SERIES, 'סיכום', `JSON הוא פורמט פשוט להעברת נתונים בין מערכות.

רואים אותו כמעט בכל מקום באינטרנט:
API מחזיר JSON, ו-Frontend או Backend יודעים לקרוא.

במילים פשוטות:

זו הדרך הסטנדרטית שבה מערכות "מדברות נתונים" —
בצורה קלה, מסודרת וקריאה.

לא נוצץ.
פשוט עובד — ולכן כולם משתמשים.`),
  ],
}
