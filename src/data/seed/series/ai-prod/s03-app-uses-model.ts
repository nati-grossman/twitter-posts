import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'איך אפליקציה משתמשת במודל?'

export const aiProdAppUsesModel: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'ארכיטקטורה', `האפליקציה במסך — React או אפליקציה אחרת — לא אמורה להחזיק את מפתח ה־AI.

הזרימה הבריאה נראית כך: App → Backend → API של AI.

השרת שומר סודות, בודק הרשאות, ורק אז קורא למודל.

אם המפתח יושב בדפדפן — מישהו יגנוב אותו מהר מאוד.

אבטחה כאן היא ארכיטקטורה מהיום הראשון, לא תוספת בסוף.

סוד בדפדפן הוא סוד של כולם.`),
    seriesPost(2, SERIES, 'פרומפט', `המודל לא "יודע לבד" מה האפליקציה שלך רוצה ממנו.

האפליקציה בונה messages: הקשר, תבנית, הוראות, ושאלת המשתמש.

שם נמצאת השליטה האמיתית על ההתנהגות שתראה במסך.

פרומפט טוב הוא חלק מהמוצר — לא משפט אקראי שנזרק ברגע האחרון.

אתה לא רק שולח טקסט.
אתה מעצב שיחה מכוונת.

מי שבונה את ההודעות — שולט בתוצאה.`),
    seriesPost(3, SERIES, 'תשובה', `כשהתשובה חוזרת מהמודל, האפליקציה עושה איתה דברים אמיתיים.

מציגה במסך, שומרת ב־DB, בונה היסטוריה ל־UI של המשתמש.

המודל סיים את חלקו: ניבוי טקסט.
המוצר רק מתחיל: הצגה, זיכרון, חוויה.

בלי השלב הזה אין אפליקציה — יש רק תיבת טקסט ריקה.

הערך נולד אחרי שהטוקנים כבר הגיעו.

בלי שמירה והצגה אין באמת מוצר.`),
    seriesPost(4, SERIES, 'כלים', `לפעמים המודל צריך מידע מהעולם של האפליקציה שלך.

ה־Backend קורא ל־DB או ל־API אחר, ומזין את התוצאה חזרה לשיחה.

זה function calling בפועל: המודל מבקש צעד, המערכת מביאה נתונים.

המודל לא נכנס ישירות למסד הנתונים שלך.
האפליקציה מתווכת ושומרת שליטה.

ככה נשמרת גם גמישות — וגם גבולות.`),
    seriesPost(5, SERIES, 'RAG', `RAG מתחיל משאלה פשוטה של משתמש.

הופכים אותה ל־embedding, מחפשים מסמכים רלוונטיים, ושמים אותם בפרומפט.

המודל עונה עם הקשר שהאפליקציה מצאה — לא מזיכרון קסום של כל העולם.

זה עבודת מוצר מסביב למודל.
חיפוש קודם, ניבוי אחר כך.

בלי השליפה נשארים עם ניחושים כלליים מדי.

הקשר טוב מתחיל בחיפוש טוב.`),
    seriesPost(6, SERIES, 'Timeout', `מודלים יכולים לקחת זמן. לפעמים יותר מדי לטעם של משתמש.

Timeout של עשרות שניות, ספינר ברור, ואפשרות ביטול — חלק מה־UX.

אם נכשלים — retry מדוד, לא לולאה אינסופית שמכבידה על כולם.

משתמש שלא יודע מה קורה מרגיש שהמערכת תקועה לגמרי.

זמן המתנה מתוכנן עדיף תמיד על שתיקה.

שקט במסך מרגיש כמו תקלה.`),
    seriesPost(7, SERIES, 'Fallback', `מה קורה כשהמודל הכבד עמוס, איטי, או פשוט נכשל?

Fallback: מודל קטן יותר, או הודעת שגיאה ידידותית שמסבירה מה קרה.

העיקר — לא קריסה שקטה של כל המסך באמצע פעולה.

מוצר טוב מתכנן את היום הרע מראש, לא רק את היום המושלם.

גיבוי הוא חלק מהארכיטקטורה, לא בושה.

יום רע מתוכנן עדיף על הפתעה.`),
    seriesPost(8, SERIES, 'לוגים', `לוגים עוזרים לדבג מהר — אבל מסוכנים אם שומרים הכול בלי לחשוב.

סודות, PII, ותוכן רגיש לא צריכים ליפול ללוג גולמי במקרה.

אנונימיזציה ותאימות הן חלק מהעיצוב, לא אחרי־מחשבה.

מה שעוזר לצוות לא חייב לחשוף את המשתמש.

לוג חכם = מספיק מטא־דאטה, הרבה פחות סודות.

פחות סודות בלוג = פחות סיכון.`),
    seriesPost(9, SERIES, 'עלות', `כל קריאה למודל עולה כסף אמיתי — גם כשהפיצ'ר "רק עובד".

מגבילים אורך, מסכמים הקשר ארוך, ושומרים cache לתשובות שחוזרות.

זה לא רק אופטימיזציה טכנית.
זה מוצר שיכול לחיות לאורך זמן.

בלי משמעת עלות, הפיצ'ר AI הופך לחור שקט בתקציב.

שליטה באורך ובחזרות חוסכת המון.

חיסכון קטן בכל קריאה מצטבר מהר.`),
    seriesPost(10, SERIES, 'סיכום', `אפליקציה בונה פרומפט, קוראת ל־API, מציגה למשתמש, ושומרת מה שצריך.

המודל רק מנבא טקסט.

הלוגיקה שלך — לא של GPT — מחליטה מה קורה לפני ואחרי הקריאה.

RAG, כלים, timeout, fallback: כל אלה שכבות מוצר מסביב.

ככה נולד שימוש אמיתי במודל, לא רק הדגמה יפה.

המודל מנבא. האפליקציה מחליטה.`),
  ],
}
