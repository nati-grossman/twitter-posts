import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'מה זה API באפליקציות?'

export const appsApiInApps: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'חוזה', `API באפליקציה הוא לא קסם מסתורי.

זה חוזה: רשימת endpoints שהאפליקציה יודעת לקרוא.
מה מבקשים, מה מחזירים, ואיך מזדהים.

האפליקציה לא ניגשת ישירות למסד הנתונים של השרת.
היא מדברת דרך הדלתות שנפתחו לה במכוון.

ככה השרת שומר שליטה — והלקוח נשאר פשוט יותר.

בלי חוזה ברור, כל צד מנחש.
עם חוזה — אפשר לבנות מסכים בביטחון.`),
    seriesPost(2, SERIES, 'דוגמה', `דוגמה קטנה ומוכרת:

GET /api/user/me

השרת מחזיר שם, תמונה, אולי הגדרות בסיסיות.
האפליקציה מציירת מזה את מסך הפרופיל.

המשתמש רואה תמונה עגולה ושם.
המפתח רואה בקשה, תשובה, ומיפוי לשדות במסך.

אותו רעיון לכל פיד, רשימה, או פרטי הזמנה.

API טוב נותן בדיוק את מה שהמסך צריך — בלי לספר יותר מדי.`),
    seriesPost(3, SERIES, 'SDK', `לא תמיד כותבים את כל ה־HTTP ביד.

יש SDK — ספרייה שעוטפת API.
למשל של Stripe לתשלומים.

אתה קורא לפונקציה ברורה יותר.
מאחורי הקלעים היא בונה בקשות, מטפלת בפורמט, ולפעמים גם בשגיאות נפוצות.

זה חוסך זמן ומקטין טעויות קטנות.

אבל עדיין חשוב להבין מה קורה מתחת.
כשה־SDK נכשל — מישהו צריך לדעת לקרוא את המפה.`),
    seriesPost(4, SERIES, 'מפתחות', `API key נשמע כמו סיסמה קטנה שפותחת שירות.

הבעיה: אם שמים מפתח סודי בתוך אפליקציה ציבורית, אפשר לחלץ אותו מה־APK.

לכן מפתחות רגישים שייכים לשרת שלך — לא לקוד שהמשתמש מוריד.

האפליקציה מדברת איתך.
אתה מדבר עם השירות החיצוני במפתח השמור.

מפתח שנשאר בצד הלקוח הוא מפתח שכמעט תמיד ייחשף בסוף.`),
    seriesPost(5, SERIES, 'OAuth', `התחברות עם Google נראית כמו כפתור אחד.

מאחוריה יש OAuth: תהליך שמביא token בלי לתת לאפליקציה את הסיסמה שלך לגוגל.

האפליקציה מקבלת אישור מוגבל.
ואז שולחת את ה־token לשרת שלך — שימשיך את הטיפול בזהירות.

זה נוח למשתמש.
וזה גם מוריד ממך אחריות על שמירת סיסמאות של אחרים.

OAuth הוא דרך להגיד "תסמוך עליי לזה" — בלי למסור את כל המפתחות לבית.`),
    seriesPost(6, SERIES, 'GraphQL', `לפעמים REST מרגיש כמו כמה נסיעות הלוך־חזור רק כדי למלא מסך אחד.

GraphQL מאפשר שאילתה אחת שבה בוחרים אילו שדות רוצים.

פחות round-trips.
יותר שליטה בידי הלקוח.

אבל יש מחיר: מורכבות בשרת, בתכנון, ובאבטחת מה מותר לשאול.

זה לא תמיד "יותר טוב".
זה כלי חזק כשיש מסכים עשירים ונתונים מקושרים.

GraphQL מחליף הרבה בקשות קטנות בשיחה מדויקת אחת — כשעושים אותו נכון.`),
    seriesPost(7, SERIES, 'Rate limit', `אפליקציה יכולה לשלוח יותר מדי בקשות בלי לשים לב.

השרת שם גבול — rate limit.
עוברים אותו? מקבלים 429, ומבקשים להאט.

ל־UX זה חשוב:
לא להציג כשל מסתורי, אלא להבין שצריך לחכות או לצמצם רעש.

Rate limit מגן על השרת — ובעקיפין גם על כולם שמשתמשים בו.

אפליקציה מנומסת יודעת לא לדפוק על הדלת בלי הפסקה.`),
    seriesPost(8, SERIES, 'Mock', `בפיתוח לא תמיד יש backend מוכן.

אז בונים mock — שרת מזויף או תשובות קבועות.
המסכים זזים, ה־UI מתקדם, בלי לחכות לכל ה־API האמיתי.

זה מאיץ עבודה.
ועוזר לבדוק מצבי קצה: שגיאה, ריק, איטיות.

בסוף מחליפים לאמיתי.
אבל הרבה מהעיצוב כבר נבדק.

Mock טוב הוא תפאורה זמנית שמאפשרת לחזרה האמיתית להגיע מוכנה.`),
    seriesPost(9, SERIES, 'OpenAPI', `כשיש צוותים — מובייל, ווב, שרת — צריך שפה משותפת לתיעוד.

OpenAPI מתאר את ה־API בצורה מסודרת:
נתיבים, שדות, תשובות.

אפשר לייצר ממנו client, לבדוק התאמה, ולסנכרן ציפיות.

בלי תיעוד חי, כל אחד עובד לפי זיכרון או צ׳אט ישן.

OpenAPI לא מחליף שיחה בין אנשים.
הוא מונע מצב שבו הקוד וההבנה מתרחקים בשקט.`),
    seriesPost(10, SERIES, 'סיכום', `API באפליקציות הוא החוזה בין המסך לשרת.

Endpoints, פורמט תשובה, ו־auth.
לפעמים דרך SDK, לפעמים GraphQL, תמיד עם גבולות כמו rate limit.

מפתחות שייכים למקום בטוח.
OAuth עוזר להתחבר בלי לפזר סיסמאות.
Mock ו־OpenAPI עוזרים לצוותים לזוז יחד.

כש־API משתנה — לפעמים גם האפליקציה חייבת להתעדכן.

החוזה הוא מה שמחזיק את המוצר מחובר.`),
  ],
}
