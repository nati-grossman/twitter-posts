import type { SeedSubtopic } from '../../types'
import { seriesPost } from '../helpers'

const SERIES = 'מה זה caching מתקדם?'

export const perfAdvancedCache: SeedSubtopic = {
  name: SERIES,
  posts: [
    seriesPost(1, SERIES, 'מעבר לדפדפן', `הרבה אנשים מכירים Cache רק מהדפדפן:

"הורדתי תמונה פעם אחת → בפעם הבאה היא נטענת מהר יותר".

אבל במערכות גדולות יש הרבה שכבות Cache, לא רק אחת:

• Browser Cache — בדפדפן של המשתמש
• CDN Cache — קרוב למשתמשים בעולם
• Application Cache — למשל Redis ליד השרת
• Database Cache — בתוך מסד הנתונים עצמו

כל שכבה חוסכת עבודה אחרת: הורדה, רשת, חישוב, או שאילתה.

המטרה תמיד זהה:
לא לבצע שוב עבודה שכבר בוצעה.

במקום לחשב, לשלוף או להוריד מחדש —
משתמשים בתוצאה קיימת.
זה ההבדל בין מערכת שמרגישה כבדה למערכת שמרגישה מיידית.`),
    seriesPost(2, SERIES, 'Redis', `אחד הכלים הנפוצים ביותר ל-Cache הוא Redis.

Redis שומר מידע בזיכרון (RAM),
ולכן קריאה ממנו בדרך כלל מהירה בהרבה מקריאה ממסד נתונים על דיסק.

לדוגמה: משתמש נכנס לפרופיל שלו.

במקום:
שרת → Database → החזרת מידע

אפשר:
שרת → Redis → החזרת מידע

ורק אם אין ב-Redis — פונים ל-Database ושומרים שם לעתיד.

משתמשים בו לדברים כמו:
• נתוני משתמשים
• Sessions
• ספירת צפיות
• Rate limiting
• תוצאות חישובים יקרים

Redis לא מחליף את מסד הנתונים — הוא מקצר את הדרך אליו כשאותה תשובה חוזרת שוב ושוב.`),
    seriesPost(3, SERIES, 'Cache-aside', `Cache-aside היא אחת השיטות הנפוצות ביותר לניהול Cache.

הזרימה פשוטה:

האפליקציה בודקת אם הנתון קיים ב-Cache.
אם כן → מחזירה אותו.
אם לא → פונה ל-Database,
שומרת את התוצאה ב-Cache,
ומחזירה למשתמש.

לדוגמה עם Redis:
Request → Check Cache → Miss → Database → Save Cache → Response

בפעם הבאה אותה בקשה לרוב תעצור ב-Hit ותחסוך את מסע ה-Database.

היתרון:
האפליקציה שולטת בדיוק במה נשמר ומתי.

חיסרון שכדאי לזכור:
בקשות ראשונות אחרי Miss עדיין משלמות את מחיר ה-Database — עד שה-Cache מתמלא.`),
    seriesPost(4, SERIES, 'Write-through', `בגישת Write-through כל כתיבה עוברת קודם דרך ה-Cache.

לדוגמה: משתמש משנה שם בפרופיל.

הזרימה:
עדכון Cache → עדכון Database → אישור למשתמש.

כך ה-Cache תמיד מעודכן אחרי כתיבה מוצלחת,
וקריאות שמגיעות מיד אחרי לא רואות ערך ישן ב-Redis או בשכבה אחרת.

יתרון:
קריאות מקבלות מידע חדש בלי "חלון" של חוסר התאמה בין Cache ל-Database.

חיסרון:
כל כתיבה איטית יותר,
כי צריך לעדכן שני מקומות במקום אחד.

בוחרים בזה כשעקביות אחרי כתיבה חשובה יותר ממהירות הכתיבה עצמה —
למשל בפרופיל משתמש או במחיר שמוצג מיד אחרי עדכון.`),
    seriesPost(5, SERIES, 'Invalidation', `הבעיה הכי קשה ב-Cache:

איך יודעים מתי המידע כבר לא נכון?

לדוגמה:
שמרנו מחיר מוצר ב-Redis.

אחר כך המחיר השתנה ב-Database ל-120 ש"ח,
אבל ב-Cache עדיין כתוב 100 ש"ח.

עכשיו יש שתי גרסאות — והמשתמש עלול לראות את הישנה.

צריך לבצע Invalidation:
למחוק או לעדכן את הערך הישן ברגע שהמקור משתנה.

בלי זה ה-Cache הופך ממאיץ לבור של באגים שקטים.

המשפט המפורסם בעולם התוכנה:

"There are only two hard things in Computer Science:
cache invalidation and naming things."

זה לא בדיחה — זה סיכום מדויק של כאב אמיתי.`),
    seriesPost(6, SERIES, 'CDN Edge', `Cache לא חייב לשבת רק ליד השרת שלכם.

CDN מפזר Cache ברחבי העולם, קרוב למשתמשים.

לדוגמה:
תמונה נמצאת בשרת המקורי בארה"ב.
משתמש בישראל מבקש אותה.

בפעם הראשונה:
ה-CDN מביא אותה מהמקור ושומר אותה ב-Edge הקרוב.

אחר כך:
משתמשים בישראל מקבלים אותה מהשרת הקרוב אליהם,
בלי לחצות שוב את האוקיינוס בכל טעינה.

התוצאה:
פחות זמן תגובה למשתמש,
ופחות עומס על שרת המקור.

CDN מתאים במיוחד לקבצים סטטיים כמו תמונות, CSS ו-JavaScript —
המקומות שבהם אותו קובץ חוזר שוב ושוב לאלפי משתמשים.`),
    seriesPost(7, SERIES, 'HTTP Cache Headers', `גם HTTP עצמו כולל מנגנון Cache מובנה.

השרת יכול להגיד לדפדפן כמה זמן מותר לשמור מידע.

לדוגמה:
Cache-Control: max-age=3600

כלומר: "שמור את הקובץ שעה".

במהלך השעה הזו הדפדפן יכול להשתמש בעותק המקומי
בלי לפנות שוב לשרת לכל טעינה.

זה חשוב במיוחד עבור:
• תמונות
• CSS
• JavaScript
• קבצים סטטיים אחרים

Header אחד נכון יכול לחסוך אלפי בקשות מיותרות ביום.
Header שגוי יכול לגרום למשתמשים לראות גרסה ישנה של האתר.

Cache ב-HTTP הוא פשוט — אבל ההשפעה שלו עצומה.`),
    seriesPost(8, SERIES, 'Memoization', `Memoization הוא Cache ברמת הפונקציה, לא ברמת השרת.

הרעיון:
אם פונקציה קיבלה אותו קלט — אל תחשב שוב.
שמור את התוצאה והחזר אותה מיד.

לדוגמה:
פונקציה יקרה בשם calculate(10).

בפעם הראשונה: מחשבת ושומרת.
בפעם השנייה עם אותו קלט: מחזירה את מה ששמרה.

זה נפוץ באלגוריתמים (כמו חישובים רקורסיביים עם חפיפה),
וגם ב-Frontend כשאותו חישוב רץ שוב ושוב ברינדור.

החיסכון לא מגיע מרשת או מ-Database —
אלא מזה שלא מבזבזים CPU על אותה עבודה פעמיים.

Memoization הוא Cache קטן ומוקדי — אבל לפעמים זה בדיוק מה שחסר.`),
    seriesPost(9, SERIES, 'Cache Stampede', `יש בעיה מעניינת שנקראת Cache Stampede.

נניח שב-Redis יש נתון פופולרי — דף בית, מחיר מוצר, או ספירה חמה.
פתאום הוא פג תוקף.

אלפי משתמשים מגיעים באותו רגע.
כולם רואים: "אין Cache".
וכולם פונים יחד ל-Database.

במקום להוריד עומס — יצרנו עומס גדול יותר בדיוק ברגע הכי רגיש.

פתרונות נפוצים:
• רק בקשה אחת תחשב מחדש; השאר מחכות לתוצאה
• הארכת זמן Cache / soft TTL
• טעינה מראש (Pre-warming) לפני שפג התוקף

Cache טוב בלי הגנה מ-Stampede
יכול לקרוס בדיוק כשהוא אמור להציל אתכם.`),
    seriesPost(10, SERIES, 'סיכום', `Cache הוא לא רק "לשמור משהו בזיכרון".

במערכות אמיתיות צריך לחשוב על:
• איפה לשמור Cache — דפדפן, CDN, Redis, או ליד הקוד
• איך לקרוא ממנו — למשל Cache-aside
• איך לעדכן אותו — למשל Write-through
• מתי למחוק אותו — Invalidation
• איך להתמודד עם עומסים — כולל Cache Stampede

כלים וטכניקות שפגשנו:
Redis, Cache-aside, Write-through, Invalidation,
CDN, HTTP Cache Headers, Memoization,
והגנה מ-Cache Stampede.

כל שכבה חוסכת סוג אחר של עבודה.
ביחד הן הופכות מערכת איטית למערכת שמרגישה מיידית —
בלי להמציא שרת חדש מהאוויר.`),
  ],
}
